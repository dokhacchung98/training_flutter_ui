import 'package:flutter/material.dart';

class ItemMovieUpcoming extends StatelessWidget {
  const ItemMovieUpcoming({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
